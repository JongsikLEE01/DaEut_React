import React from 'react';
import LoginForm from '../../components/auth/LoginForm';

const Login = () => {
    return (
        <div>
            <h2>로그인</h2>
            <LoginForm />
        </div>
    );
};

export default Login;
